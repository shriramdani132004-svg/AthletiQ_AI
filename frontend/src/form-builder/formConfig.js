export const OPTION_FIELD_TYPES = Object.freeze([
    "DROPDOWN",
    "RADIO",
    "CHECKBOX",
    "MULTI_SELECT"
]);

export const UPLOAD_FIELD_TYPES = Object.freeze([
    "FILE",
    "IMAGE"
]);

export const VALIDATION_FIELD_TYPES = Object.freeze([
    "TEXT",
    "NUMBER",
    "LONG_TEXT",
    "EMAIL",
    "PHONE",
    "URL",
    "FILE",
    "IMAGE",
    "RATING"
]);

export const DEFAULT_UPLOAD_TYPES = Object.freeze([
    "pdf",
    "jpg",
    "jpeg",
    "png"
]);

export const DEFAULT_RATING_RANGE = Object.freeze({
    min: 1,
    max: 5
});